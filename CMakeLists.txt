PROJECT(estimate_wall_orientation)
cmake_minimum_required(VERSION 2.6)

SET (CMAKE_MODULE_PATH "${CMAKE_SOURCE_DIR}/.orogen/config")
INCLUDE(estimate_wall_orientationBase)

# FIND_PACKAGE(KDL)
# FIND_PACKAGE(OCL)

