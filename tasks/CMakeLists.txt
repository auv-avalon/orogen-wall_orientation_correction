# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(wall_orientation_correctionTaskLib)
ADD_LIBRARY(${WALL_ORIENTATION_CORRECTION_TASKLIB_NAME} SHARED 
    ${WALL_ORIENTATION_CORRECTION_TASKLIB_SOURCES})
add_dependencies(${WALL_ORIENTATION_CORRECTION_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${WALL_ORIENTATION_CORRECTION_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${WALL_ORIENTATION_CORRECTION_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${WALL_ORIENTATION_CORRECTION_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${WALL_ORIENTATION_CORRECTION_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${WALL_ORIENTATION_CORRECTION_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${WALL_ORIENTATION_CORRECTION_TASKLIB_HEADERS}
    DESTINATION include/orocos/wall_orientation_correction)

