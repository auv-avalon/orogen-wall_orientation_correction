# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(estimate_wall_orientationTaskLib)
ADD_LIBRARY(${ESTIMATE_WALL_ORIENTATION_TASKLIB_NAME} SHARED 
    ${ESTIMATE_WALL_ORIENTATION_TASKLIB_SOURCES})
add_dependencies(${ESTIMATE_WALL_ORIENTATION_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${ESTIMATE_WALL_ORIENTATION_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${ESTIMATE_WALL_ORIENTATION_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${ESTIMATE_WALL_ORIENTATION_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${ESTIMATE_WALL_ORIENTATION_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${ESTIMATE_WALL_ORIENTATION_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${ESTIMATE_WALL_ORIENTATION_TASKLIB_HEADERS}
    DESTINATION include/orocos/estimate_wall_orientation)

